<Window x:Class="UofM.HCI.tPab.TPadWindow" x:Name="tpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:converters="clr-namespace:UofM.HCI.tPab.Converters"
        WindowStyle="ToolWindow" ResizeMode="NoResize" SizeToContent="WidthAndHeight" Topmost="True" WindowStartupLocation="Manual"
        Loaded="tpWindow_Loaded" SizeChanged="tpWindow_SizeChanged">
  
  <Window.Resources>
    <converters:LayerScalingConverter x:Key="cMultiplier" />
  </Window.Resources>
  
  <Grid x:Name="gTPadApp">
    <Grid.Width>
      <MultiBinding Converter="{StaticResource ResourceKey=cMultiplier}">
        <Binding ElementName="tpWindow" Path="Core.Profile.Resolution.Width"/>
        <Binding ElementName="tpWindow" Path="SizeMultiplier"/>
      </MultiBinding>
    </Grid.Width>
    <Grid.Height>
      <MultiBinding Converter="{StaticResource ResourceKey=cMultiplier}">
        <Binding ElementName="tpWindow" Path="Core.Profile.Resolution.Height"/>
        <Binding ElementName="tpWindow" Path="SizeMultiplier"/>
      </MultiBinding>      
    </Grid.Height>
  </Grid>
</Window>
