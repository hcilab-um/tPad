<UserControl x:Class="UofM.HCI.tPab.App.ActiveReader.StickyNote" x:Name="stickyNote"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="150" d:DesignWidth="150" Loaded="stickyNote_Loaded">

  <UserControl.RenderTransform>
    <ScaleTransform ScaleX="{Binding ElementName=stickyNote, Path=InvWidthFactor}" 
                    ScaleY="{Binding ElementName=stickyNote, Path=InvHeightFactor}"/>
  </UserControl.RenderTransform>
  
  <Grid x:Name="gNote" Background="LightYellow" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
    <TextBox x:Name="tNote" Margin="0 0 20 0" Background="LightYellow" BorderThickness="0" Grid.Column="0"
             VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
    <Button x:Name="bClose" HorizontalAlignment="Right" VerticalAlignment="Top" Width="20" Height="20" Grid.Column="1" Background="Transparent" BorderThickness="0" BorderBrush="Transparent">
      <DockPanel>
        <Image Source="Images/delete.png"/>
      </DockPanel>
    </Button>
    <Image x:Name="bResize" Source="Images/resize.png" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="20" Height="20" Grid.Column="1"/>

  </Grid>

</UserControl>
